import { Card } from '@/components/ui/card';
import { cn } from '@/lib/utils';
import React from 'react';

const Payments = () => {
  return <Card className={cn('w-[37.5%] h-[100%] bg-[#000]')}></Card>;
};

export default Payments;
